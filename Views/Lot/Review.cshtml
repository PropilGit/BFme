@using BFme.Models
@using BFme.ViewModels
@model RieltorViewModel
@{
}


<!-- Lot-Rieltor -->
<div class="container border rounded mt-5 pt-3" style="background-color: #c2ffe0">
    @using (Html.BeginForm("EditReview", "Lot", FormMethod.Post))
    {
        <fieldset>

            <!-- Form Name -->
            <legend>Данные по анализу рынка</legend>

            <h4>@Model.Name</h4>

            <!-- Id -->
            <input type="hidden" value="@Model.Id" name="Id" />

            <!-- MarketCost -->
            <div class="form-group row">
                <label class="col-md-2 control-label" for="MarketCost">Рыночная цена, руб</label>
                <div class="col-md-10">
                    <input type="text" value="@Model.MarketCost" name="MarketCost" class="form-control input-md">

                </div>
            </div>

            <!-- Review -->
            <div class="form-group row">
                <label class="col-md-2 control-label" for="Review">Описание</label>
                <div class="col-md-10">
                    <textarea class="form-control" name="Review">@Model.Review</textarea>
                </div>
            </div>

            <!-- Сохранить -->
            <div class="form-group row">
                <div class="col"></div>
                <div class="col-2">
                    <input value="Сохранить" type="submit" class="form-control input-md">
                </div>
            </div>

        </fieldset>
    }
</div>
